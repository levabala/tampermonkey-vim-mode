{
  "name": "tampermonkey-vim-mode",
  "version": "1.0.10",
  "description": "Vim-like editing for textareas and inputs",
  "type": "module",
  "scripts": {
    "test": "bunx vitest run",
    "test:watch": "bunx vitest",
    "test:ui": "bunx vitest --ui",
    "test:coverage": "bunx vitest --coverage"
  },
  "devDependencies": {
    "vitest": "^2.1.8",
    "jsdom": "^25.0.1",
    "@vitest/ui": "^2.1.8"
  }
}
